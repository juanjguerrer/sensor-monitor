query GetSensorDetail($id: Int!, $limit: Int) {
  sensor(id: $id) {
    id
    name
    unit
    type
    location {
      id
      name
    }
  }
  readings(sensorId: $id, limit: $limit) {
    id
    value
    recordedAt
  }
}